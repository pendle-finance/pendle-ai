description: "All Pendle skill evals"

# Run all suites at once or individual suites via Turbo:
#   turbo run eval
#   turbo run eval --filter=@pendle-ai/eval-suite-pendle-swap

import:
  - suites/pendle-swap/promptfoo.yaml
  - suites/pendle-data/promptfoo.yaml
  - suites/pendle-portfolio/promptfoo.yaml
  - suites/pendle-order/promptfoo.yaml
  # Layer 2: boots MCP server and validates tool responses against live API
  # Note: excluded from default run — use eval-suite-mcp-tools separately
  # (requires built mcp-server and hits the live Pendle API)
